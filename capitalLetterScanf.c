/*
#include <stdio.h> 
int main() 
{ 
  char str[100]; 
  scanf("%[A-Z ]s", str); 
  printf ("%s", str); 
  return 0; 
} 
// -----------------------------------------------------------------------------
#include <stdio.h> 
 
int main() { 
    char input[100];  // Adjust the size as needed 
 
    printf("Enter only capital letters and spaces: "); 
    scanf("%[A-Z ]", input);  // Reads capital letters and spaces 
 
    // To clear the input buffer until a newline (if needed) 
    while (getchar() != '\n'); 
 
    printf("You entered: %s\n", input); 
    return 0; 
} */

#include <stdio.h>

int mian()
{
    char c;
    printf("Enter any one alphabet");
    
}